@import '../../../style/mixins'

.wrapper
	min-width 25px
	min-height 25px
	// position relative
	// cursor pointer
	// display inline-block
	display inline-flex
	align-items center
	align-content center

.icon
	flex-grow 1
	flex-shrink 1
	// width 100%
	// height 100%
	// position absolute
	// top 50%
	// left 50%
	// transform translate(-50%, -50%)

svg-colors()
